language: java

jdk:
  - oraclejdk8

cache:
  directories:
    - $HOME/.m2

before_install:
  - chmod +x ./mvnw

before_deploy:
  - mvn package -Dspring.profiles.active=azure
  - mkdir webapps
  - cp target/ROOT.war webapps/

deploy:
  provider: azure_web_apps
  skip_cleanup: true
